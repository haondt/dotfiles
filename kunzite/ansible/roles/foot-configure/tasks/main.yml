- name: Ensure the foot configuration directory exists
  file:
    path: "{{ ansible_facts['user_dir'] }}/.config/foot"
    state: directory
- name: Deploy foot.ini
  template:
    src: foot.ini.j2
    dest: "{{ ansible_facts['user_dir'] }}/.config/foot/foot.ini"
