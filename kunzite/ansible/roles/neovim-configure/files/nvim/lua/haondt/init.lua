require("haondt.remap")       -- must be done before loading lazy
require("haondt.colors")
require("haondt.colorscheme") -- also must be before lazy
require("haondt.set")         -- also before lazy
require("haondt.lazy")
require("haondt.git")
require("haondt.diff")
require("haondt.json").setup()
